#define ll long long
#include <bits/stdc++.h>
using namespace std;
#define rep(n) for (size_t i=0; i < n; i++)

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    return (0);
}
